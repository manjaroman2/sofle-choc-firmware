- Always wants the `/ponytail` skill invoked for tasks in this project; states this as a standing rule ("always use the skill /ponytail"). Confidence: 0.85
- Treats the ponytail skill rule as session-overridable: may explicitly suspend it ("don't use ponytail skill for this session") — honor a per-session opt-out rather than applying the standing rule unconditionally. Confidence: 0.5
- When a change causes a regression, prefers a root-cause investigation (diagnose the exact offending line and explain why) over reverting or patching the symptom. Confidence: 0.65
- Prefers to keep an intended behavior/performance improvement (e.g. a faster scroll rate) rather than revert to the last known-good setting when a regression appears; willing to take on extra implementation work (new flash table, regenerated assets) to fix it properly while preserving the improvement. Confidence: 0.5
- Prefers alternative/unused code paths (e.g. a second build mode) to be made genuinely functional and testable rather than left unbuilt or skipped, so tradeoffs can be measured empirically instead of asserted. Confidence: 0.5
- Prioritizes legibility/visual separation in rendered output over tight packing: explicitly wants a clear gap between glyphs (specified 4 columns) so characters "don't melt into each other". Confidence: 0.5
- Expects changes to be scoped exactly to the reported symptom, not generalized: rejected a blanket inter-glyph change and demanded confirmation that the gap occurs only where intended (the wrap seam between the trailing '!' and the wrapped-around first character). Confidence: 0.6
- Insists the agent empirically verify claimed behavior (render/instrument/measure the actual output) rather than assert it from reasoning — e.g. "verify that the new gap only happens at the end of the print_Text text". Confidence: 0.65
- Prioritizes performance/latency over visual polish: explicitly willing to accept render artifacts (e.g. visibly steppy "jumps" in a scrolling animation) if that is the price of a faster scroll rate. Confidence: 0.6
- Prefers new capabilities (e.g. double buffering for the display) be added as an optional, opt-in switch (compile-time flag) that leaves the existing/default path untouched, rather than replacing it outright. Confidence: 0.55
- When a tuning knob stops scaling (lowering a delay below some point has no effect), expects the agent to identify and explain the real hard limit/bottleneck (and why), then propose concrete levers to work around it — not keep tuning the knob or hand-wave. Confidence: 0.6
